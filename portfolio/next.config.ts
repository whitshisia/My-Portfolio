import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  dir: './src', // This tells Next.js to look for pages/app in the `src` folder

};

export default nextConfig;
